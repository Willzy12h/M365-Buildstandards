# Standard 2026.09.12 validation record

All tests and interface evidence below use synthetic fixtures. No tenant authentication, consent, Graph/Exchange call, device operation, existing client evidence or saved connection data was used. Microsoft references establish documented contracts, not observed service behaviour.

| Phase | Observed local result |
|---|---|
| 0 | Strict solution and harness builds: 0 warnings, 0 errors. Engine 581 passed, app 57 passed, none failed/skipped. Harness: 247 named controls, 22 readable tables, 3667 text / 125 input contrast checks, 4276 clipping checks, 1842 keyboard stops, 10 dialog checks, 48 registered commands exercised (57 completed presses, four expected refusals), 61 not pressed, 68 images / 42 page-size combinations, zero binding issues. Fresh review and corrections are in ASTRA-REVIEW-2026-09-25.md. |
| 1 | Primary-source route investigation and INT-030–033 recorded before implementation. Exchange execution fallback chosen because documented internal module retries cannot satisfy the uncertain-write boundary. |
| 2 | Strict solution and harness builds: 0 warnings, 0 errors. Engine 668 passed, app 60 passed, none failed/skipped. New tests cover public CIDR refusal classes, stable office identity, schema compatibility, exact Windows/mobile payloads, typed confirmation, all six new reviewed actions, transport methods and no retry, read-only refusal, durable intent/persistence failure, drift, mandatory reviewers and recorded empty-group ownership. Harness: 247 named controls, 22 readable tables, 3667 text / 125 input contrast checks, 4276 clipping checks, 1843 keyboard stops, 10 dialog checks; 48 commands exercised (57 completed presses, four expected refusals), 61 not pressed; 68 images / 42 combinations; zero binding issues. The harness still uses the prior default release until the area-interface phase switches the default. |

The four phase 2 command refusals were: application setup before sign-in; deviation save without a selection; input save against the deliberately duplicated synthetic client; import without a candidate name. Tenant-facing commands were registered but not pressed. No test was deleted or weakened. Initial new-test compilation errors were corrected before the recorded passing run.

## Phase 3

Strict solution and harness builds passed with 0 warnings and 0 errors. Engine: 668 passed; application: 68 passed; no failures or skips. Tests cover combined assessment filters, selection confined to the displayed area, clearing selection on area change, legacy area presentation and distinct office manual-check records. The default release is now .12.

Harness: 253 named operable controls; 22 readable tables; 3673 text and 131 input contrast checks; 4297 clipping checks; 14 pages / 22 tab views / 2533 keyboard stops; 10 confirmation checks; 48 commands exercised, 57 completed presses, four expected refusals and 61 intentionally not pressed; 68 images / 42 page-size combinations; zero binding issues. Commands, refusal reasons and tab-order output were read. The initial vertically stacked Plan filter clipped prerequisite guidance at the two smaller sizes; a compact toolbar corrected the layout without relaxing any harness assertion. New-test assertion syntax was adjusted to the existing xUnit analyser requirements without changing the asserted behaviour.

CI phase 2: head `f4d289f761f901341e3c6564a664f11b09a49757`, [run 36119673412](https://github.com/Willzy12h/M365-Buildstandards/actions/runs/36119673412), completed successfully.

CI is checked separately against each published head; local results alone do not establish a green pull request or live validation. The manifest is generated by the existing build script and includes the new file; published standard bytes are unchanged.

## Phase 4

Phase 3 CI completed successfully at `4e140c173b5deeedb9ad2ad700e5e47ac346b71b`: [run 36120675611](https://github.com/Willzy12h/M365-Buildstandards/actions/runs/36120675611).

Strict solution and harness builds: 0 warnings, 0 errors. Engine: **721 passed**; application: **72 passed**; no failures or skips. Exchange tests cover all ten controls, three tenant identity checks, strict provenance and JSON projection, incomplete reads, inert imported snapshots, accepted-domain binding, both DKIM selectors, stale/ambiguous/malformed DNS, mandatory domain input, fake DNS transport and preservation in all engineer report formats. The new application fixture initially omitted its input dictionary; correcting fixture initialisation left every product assertion intact. No existing test was deleted or weakened.

Harness: 262 named operable controls; 22 readable tables; 3838 text and 137 input contrast checks; 4540 clipping checks; 14 pages / 23 tab views / 2697 keyboard stops; 10 confirmation checks; 51 commands exercised, 60 completed presses, four expected refusals and 63 intentionally not pressed; 71 images / 42 page-size combinations; zero binding issues. The three new tab screenshots were generated and the smallest layout inspected. The DNS action and file chooser were registered with explicit not-pressed reasons. All captures, files and exports were synthetic.

The generated read-only PowerShell was parsed offline with zero syntax errors and its command inventory inspected. The first parse attempt was blocked by the shell's constrained language mode; a permitted full-language parse was then run successfully. The script was **not executed**. No module connection, DNS query or tenant operation was performed. Microsoft behaviour remains unverified as described in [Exchange and Purview observations](../EXCHANGE-PURVIEW.md).

## Phase 5

Phase 4 CI completed successfully at `6f3a851f9084a6c85ebd62c308d032c201762ea3`: [run 36125380149](https://github.com/Willzy12h/M365-Buildstandards/actions/runs/36125380149).

The INT-030/036 fallback is implemented: selected, commented PowerShell proposals, with no Exchange write execution. Strict solution and harness builds: 0 warnings, 0 errors. Engine: **753 passed**; application: **73 passed**; no failures or skips. Tests assert every exported proposal is inert and selective; require matching typed tenant and domain, intact durable before evidence, complete/fresh observations and an accepted domain; refuse changed evidence; require both DKIM selectors; restrict missing-DKIM creation and the SPF bypass to disabled candidates; keep DMARC/retention report-only. Each catalogue write reference is preceded by the manual tenant check. No test or assertion was removed or relaxed.

Harness: 268 named operable controls; 22 readable tables; 3847 text and 143 input contrast checks; 4561 clipping checks; 14 pages / 23 tab views / 2703 keyboard stops; 10 confirmation checks; 52 commands exercised, 61 completed presses, four expected refusals, 63 not pressed; 71 images / 42 combinations; zero binding issues. The selected proposal command completed using a synthetic saved capture. Initial harness compilation identified a missing namespace and the existing static-initialiser nullability convention; both were corrected before the passing run. All ten catalogue PowerShell references were parsed offline with zero syntax errors; they were not executed.

After adding an explicit pre-read Purview tenant check to its manual reference, all 32 proposal tests were rerun and passed. All ten references were parsed again; the generated synthetic proposal parsed successfully and contained **zero executable command ASTs**. No script was executed.

## Phase 6

Phase 5 CI completed successfully at `61575062e2786290a899e4910828b7ea23318367`: [run 36127313107](https://github.com/Willzy12h/M365-Buildstandards/actions/runs/36127313107).

Both engineer documents now generate in HTML and Markdown from the catalogue, with all **96 controls** grouped by area. Every control has before, portal, PowerShell and after/pass sections and a primary Microsoft reference. The generation API accepts no client profile, snapshot or connection. Tests cover completeness/refusal, historical releases, exact settings, section/area order, output isolation, HTML escaping, embedded Markdown fences and manual identity-change boundaries. Stale long-path optional wording and deferred classic Autopilot/Defender onboarding instructions were corrected only in .12.

Strict solution and harness builds: **0 warnings, 0 errors**. Engine: **767 passed**; application: **73 passed**; no failures or skips. All **96** PowerShell references passed an offline syntax parse, with zero errors; none was executed. The initial new document-order test split on a substring also present in lower-level headings; anchoring it to level-three headings corrected the test parser while preserving all ordering assertions.

Harness: **287** named operable controls; **22** readable tables; **4027** text and **155** input contrast checks; **4810** clipping checks; **14** pages / **24** tab views / **2738** keyboard stops; **10** confirmation checks; **56** registered commands exercised, **65** completed presses, **four** expected refusals and **63** intentionally not pressed; **74** images / **42** page-size combinations; **zero** binding issues. All four new local document exports completed. Expanded export panels were measured at all three sizes, walked by keyboard, and the smallest screenshot was inspected. Commands/refusal reasons and tab-order output were read.

The browser URL policy blocked opening a generated local HTML file. Browser appearance is therefore **unverified**; no alternate browser or policy workaround was attempted. Generated structure/content and the WPF export interface were checked separately. No tenant, DNS or device operation was performed and no existing client data was read.
