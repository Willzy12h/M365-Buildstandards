<UserControl x:Class="BDIT.TenantToolkit.App.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0">
      <TextBlock Text="Current tenant configuration" Style="{StaticResource H1}"/>
      <TextBlock Style="{StaticResource Lead}" Text="Read, inspect and export the tenant before considering any change. Reading is read-only; nothing is inferred from collections that fail."/>
      <Border Style="{StaticResource Panel}">
        <StackPanel>
          <TextBlock Text="{Binding SnapshotText}"/>
          <WrapPanel Margin="0,10,0,0">
            <Button Content="Read tenant configuration" Command="{Binding CaptureCommand}"/>
            <Button Content="Export JSON" Command="{Binding ExportJsonCommand}" Style="{StaticResource SecondaryButton}"/>
            <Button Content="Export CSV (zip)" Command="{Binding ExportCsvCommand}" Style="{StaticResource SecondaryButton}"/>
            <Button Content="Export Excel" Command="{Binding ExportXlsxCommand}" Style="{StaticResource SecondaryButton}"/>
            <ComboBox ItemsSource="{Binding StoredSnapshots}" SelectedItem="{Binding SelectedStored, Mode=TwoWay}" DisplayMemberPath="CapturedAt" Width="230" Margin="12,0,8,8"/>
            <Button Content="Open stored capture (offline)" Command="{Binding LoadStoredCommand}" Style="{StaticResource SecondaryButton}"/>
          </WrapPanel>
          <StackPanel Orientation="Horizontal">
            <TextBox Text="{Binding LastExport, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent"
                     Foreground="{StaticResource TextMuted}" FontSize="12" TextWrapping="Wrap" MaxWidth="760"/>
            <Button Content="Open containing folder" Command="{Binding OpenExportCommand}" Style="{StaticResource SecondaryButton}" Margin="10,0,0,0" Padding="8,4"/>
            <Button Content="Copy" Command="{Binding CopySummaryCommand}" Style="{StaticResource SecondaryButton}" Padding="8,4"/>
          </StackPanel>
        </StackPanel>
      </Border>
    </StackPanel>
    <Expander Grid.Row="1" Header="Collection status" IsExpanded="True" Margin="0,0,0,10">
      <DataGrid ItemsSource="{Binding Collections}" MaxHeight="220">
        <DataGrid.Columns>
          <DataGridTextColumn Header="Collection" Binding="{Binding Collection}" Width="260"/>
          <DataGridTextColumn Header="API" Binding="{Binding Api}" Width="60"/>
          <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="150"/>
          <DataGridTextColumn Header="Objects" Binding="{Binding Count}" Width="70"/>
          <DataGridTextColumn Header="Detail" Binding="{Binding Detail}" Width="*"/>
        </DataGrid.Columns>
      </DataGrid>
    </Expander>
    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="3*"/>
        <ColumnDefinition Width="2*"/>
      </Grid.ColumnDefinitions>
      <DockPanel Grid.Column="0" Margin="0,0,14,0">
        <WrapPanel DockPanel.Dock="Top" Margin="0,0,0,6">
          <TextBlock Text="Category" VerticalAlignment="Center" Margin="0,0,6,10"/>
          <ComboBox ItemsSource="{Binding CollectionFilters}" SelectedItem="{Binding FilterCollection, Mode=TwoWay}" Width="260" Margin="0,0,12,10"/>
          <TextBlock Text="Search" VerticalAlignment="Center" Margin="0,0,6,10"/>
          <TextBox Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" Width="240"/>
        </WrapPanel>
        <DataGrid ItemsSource="{Binding Objects}" SelectedItem="{Binding SelectedObject, Mode=TwoWay}">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Collection" Binding="{Binding Collection}" Width="160"/>
            <DataGridTextColumn Header="Object" Binding="{Binding Name}" Width="220"/>
            <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="160"/>
            <DataGridTextColumn Header="State" Binding="{Binding State}" Width="90"/>
            <DataGridTextColumn Header="Targeting / assignments" Binding="{Binding Targeting}" Width="*"/>
          </DataGrid.Columns>
        </DataGrid>
      </DockPanel>
      <DockPanel Grid.Column="1">
        <TextBlock DockPanel.Dock="Top" Text="Selected object settings (IDs resolved to names where the capture allows)" Style="{StaticResource Muted}" Margin="0,0,0,6"/>
        <Expander DockPanel.Dock="Bottom" Header="Raw JSON" Margin="0,8,0,0">
          <TextBox Text="{Binding SelectedObjectJson, Mode=OneWay}" IsReadOnly="True" FontFamily="Consolas" FontSize="11" Height="220" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>
        </Expander>
        <DataGrid ItemsSource="{Binding SelectedSettings}">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Setting" Binding="{Binding Setting}" Width="220"/>
            <DataGridTextColumn Header="Value" Binding="{Binding Current}" Width="*"/>
          </DataGrid.Columns>
        </DataGrid>
      </DockPanel>
    </Grid>
  </Grid>
</UserControl>
